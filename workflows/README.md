# Workflow

Workflow 是包含多个步骤、工具或产物的完整流程，通常依赖同一对话中的多轮交互。任务结束后应开启新对话执行下一次任务。

## 内容列表

尚未添加 Workflow。

## 条目格式

```text
workflows/<workflow-name>/
├── README.md
└── resources/    # 仅在需要时创建
```

条目正文应说明用途、适用场景、预期产物、操作步骤和完成条件。
